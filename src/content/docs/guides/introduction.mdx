---
title: Introduction
description: Official webpage for Gitka documentation
---

# Gitka Documentation

Welcome to the Gitka documentation! Gitka is a Ventoy-inspired tool that creates compressed, physical-media (USB/CD) local copies of all your GitHub/GitFlare repos — with offline commit capability and LAN-based sharing.

## What is Gitka?

Gitka keeps all your repos compressed on removable media. When you need to work on one, it extracts temporarily, lets you commit offline, then recompresses when you're done.

### Key Features
- **Aggressive compression** — auto-selects zstd tier based on available space
- **Offline commits** — extract a repo, commit locally, sync later
- **Smart sync** — auto-merges diverged branches with no conflicts, detects default branch automatically
- **LAN sharing** — serve a repo to others on your network via GitFlare
- **Recovery records** — optional par2 redundancy for corruption protection
- **Crash resilience** — detects orphaned extractions and recompresses automatically
- **Volume splitting** — split archives into fixed-size parts for CD/DVD or FAT32 limits
- **Cross-repo dedup** — share common blobs across repos to save space
- **AES-256-GCM encryption** — encrypt archives for secure transport
- **Host-side extraction** — extract to host computer instead of USB for better performance
- **Audit trail** — track commits, files touched, and session history per repo
- **JSON output** — all commands support `--json` for scripting and CI integration
- **Private repo support** — authenticate with personal access tokens for private repos

## Getting Started
- Check out the [Installation Guide](/gitka/guides/installation/)
- Learn basic usage in the [Usage Guide](/gitka/guides/usage/)
- Explore [Advanced Features](/gitka/reference/features/)
- View the [Configuration](/gitka/reference/configuration/) options
- See the full [CLI Command Reference](/gitka/reference/cli-reference/)
